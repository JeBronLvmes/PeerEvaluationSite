<-- Created by Jeb Alawi 7/19/18 -->
<%= render "partials/navigation_dev" %>
<p id="notice"><%= notice %></p>

<h1>Courses</h1>

<table>
  <thead>
  <tr>
    <th>id</th>
    <th>name</th>
  </tr>
  </thead>

  <tbody>
  <% @courses.each do |course| %>
    <tr>
      <td><%= course.id %></td>
      <td><%= course.name %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>